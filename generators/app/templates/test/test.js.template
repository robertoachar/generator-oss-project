'use strict';

const lib = require('../src');

describe('TESTS', () => {

  test('Basic test', (done) => {
    lib.hello((err, message) => {
      expect(err).toBeNull();
      expect(message).toBe('It works!');

      done();
    });
  });

});
